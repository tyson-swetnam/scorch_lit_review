# SCORCH Literature Review System — Python dependencies
#
# Pin major versions; let patch/minor float. Install with:
#   pip install -r requirements.txt

anthropic>=0.69            # Claude API (extraction, citations, structured outputs)
duckdb>=1.1                # Analytical database + Parquet export
jsonschema>=4.21           # Validate reviews against scorch_extraction_schema.json
pydantic>=2.6              # Typed models for structured outputs
PyYAML>=6.0                # OKF YAML frontmatter (read/write)
